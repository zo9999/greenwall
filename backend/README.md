# Triple Backend

Python Flask skeleton.

## Getting started

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Server runs on [http://localhost:5000](http://localhost:5000) and `GET /` returns `/dev/skeleton`.
